.leftcontents
  .leftcontents__user
    %h3.header__name
      = current_user.name
    %ul.leftcontents__user__list
      %li.list
        = link_to new_group_path do
          = fa_icon "edit"
      %li.list
        = link_to edit_user_path(current_user) do
          = fa_icon "cog"
  .leftcontents__groups
    - current_user.groups.each do |group|
      .leftcontents__groups__list
        = link_to group_messages_path(group) do
          .group__name
            = group.name
          .group__message
            メッセージはまだありません。

